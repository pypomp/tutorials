\documentclass[twoside,11pt]{article}
\usepackage{jmlr2e}

% Any additional header includes from YAML
$header-includes$

$highlighting-macros$

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

$if(title)$
\title{$title$}
$endif$

\author{%
$for(by-author)$
  $if(by-author.name.given)$
    \name $by-author.name.literal$%
    $if(by-author.affiliations)$
      \textsuperscript{$for(by-author.affiliations)$$it.number$$sep$,$endfor$}%
    $endif$
    $if(by-author.email)$ \email $by-author.email$$endif$ \\
  $else$
    $if(by-author.name.literal)$
      $by-author.name.literal$ \\
    $else$
      $by-author$ \\
    $endif$
  $endif$
$endfor$
\AND
\addr $for(affiliations)$\textsuperscript{$it.number$}$it.name$$if(it.department)$, $it.department$$endif$$sep$; $endfor$
}

\editor{} % Set editor here if needed

\begin{document}

\maketitle

$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

$if(keywords)$
\begin{keywords}
$for(keywords)$$keywords$$sep$, $endfor$
\end{keywords}
$endif$

$body$

$if(natbib)$
$if(bibliography)$
\bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}
$endif$
$endif$

\end{document}
